# خطة اختبار شاملة + اختبار التحمل لـ 300 مستخدم — Teachers Manager

## الخلاصة السريعة (اقرئيها أول حاجة)

فحصت الكود بالكامل، وفي **3 مستويات** للمشكلة:

1. **إصلاحات فعلية عملتها دلوقتي في الكود** — تقلل الحمل على n8n مباشرة (تفاصيلها تحت).
2. **خطر معماري حقيقي**: n8n بتاعك مستضاف على **Hugging Face Spaces** (`tasneemahmed-n8n.hf.space`) — ده على الأغلب هو السبب الأساسي في إن البرنامج "بطيء بالفعل" حتى بعدد مستخدمين قليل، ومش هيتحمل 300 شخص متزامنين مهما حسّنّا الكود، **إلا لو غيّرتِ الاستضافة**.
3. **خطة اختبار كاملة** (وظيفي + تحمل) عشان تتأكدي بالأرقام مش بالتخمين.

---

## الجزء 1: إصلاحات أداء تمت في الكود الآن

### أ) كانت الصفحة الرئيسية بتنادي على `/check-subscription` **مرتين** في نفس التحميل
مرة من نظام قفل الاشتراك (اللي بيشتغل في كل صفحة)، ومرة من بانر التجربة المجانية في لوحة التحكم تحديدًا. يعني كل فتح للوحة التحكم = ضغط مضاعف بدون داعي.

**الحل:** أضفت دالة واحدة مشتركة (`getSubscriptionCached`) في `js/sidebar.js`:
- لو فيه طلب شغال بالفعل في نفس اللحظة، أي نداء تاني **يشترك في نفس الطلب** بدل ما يبعت طلب مستقل لـ n8n.
- بعد أول استعلام ناجح، بيتخزن في `sessionStorage` لمدة **3 دقائق**، فمش هيتكرر الاستعلام مع كل تنقل بين الصفحات لنفس المستخدم.

**الأثر المتوقع:** تقليل استدعاءات `/check-subscription` بنسبة كبيرة (يمكن 70-90% حسب سلوك المستخدم في التنقل).

### ب) إضافة "موعد يومي متكرر" كانت بتبعت 7 طلبات **متتالية** (الواحد يستنى اللي قبله)
لو كل طلب ياخد نص ثانية، إضافة موعد يومي واحد كانت تاخد حوالي 3.5 ثانية.

**الحل:** أصبحت الـ 7 طلبات تتبعت **بالتوازي** (`Promise.allSettled`) بدل التتابع، لأن كل يوم مستقل تمامًا عن التاني (فحص التعارض مبني على `day_of_week` منفصل لكل طلب، فمفيش تضارب بينهم).

**الأثر المتوقع:** زمن إضافة الموعد اليومي يقرب من زمن طلب واحد بدل 7 أضعافه.

> ⚠️ ملحوظة مهمة: التوازي ده بيحسّن التجربة من ناحية المتصفح (الوقت اللي المستخدمة بتستناه)، لكن لو السيرفر (n8n) نفسه ضعيف، السبع طلبات المتزامنة ممكن تضغط عليه في نفس اللحظة بدل ما تتوزع. الحل الحقيقي الجذري هو تحسين استضافة n8n (الجزء 2 تحت).

---

## الجزء 2: الخطر الأكبر — استضافة n8n على Hugging Face Spaces

لقيت في `js/config.js`:
```
N8N_WEBHOOK_BASE: 'https://tasneemahmed-n8n.hf.space/webhook'
```

**Hugging Face Spaces مش مبنية لتستضيف API إنتاجي بيستقبل مئات الطلبات المتزامنة.** المشاكل المتوقعة:

| المشكلة | التأثير |
|---|---|
| **Cold Start** — لو الـ Space نايم (بعد فترة عدم استخدام)، أول طلب بياخد من 10 لـ 60+ ثانية لحد ما "يصحى" | أول مستخدمة تفتح البرنامج الصبح هتحس إنه "معلّق" |
| **موارد محدودة** (CPU/RAM قليلة على الخطط المجانية أو الأساسية) | مع زيادة المستخدمين، كل الطلبات بتتزاحم على نفس الموارد المحدودة دي |
| **n8n في وضع "Main Process" الافتراضي** (مش Queue Mode) | كل تنفيذات الـ workflows بتتم على نفس الـ process الواحد — مفيش توزيع حمل حقيقي على أكتر من worker |
| **قاعدة بيانات Postgres على Supabase** | غالبًا مش هي المشكلة (Supabase قوي نسبيًا)، لكن اتصالات كتير متزامنة من n8n ممكن تستهلك حد الاتصالات المسموح بيه في خطتك |

### توصيتي الصريحة
لو فعلاً بتستهدفي 300 مستخدم فعلي (مش 300 مرة واحدة بالظبط، لكن حمل حقيقي يومي)، **لازم** تنقلي n8n من Hugging Face Space إلى استضافة مخصصة لـ production، زي:
- **n8n Cloud** (الخدمة الرسمية من فريق n8n — أسهل حل وأقله صيانة).
- أو **VPS مخصص** (DigitalOcean / Hetzner / Railway / Render) بمواصفات كافية (2 vCPU / 4GB RAM على الأقل كبداية)، مع تفعيل **Queue Mode** في n8n لو الحمل كبير (يفصل استقبال الطلبات عن تنفيذها عبر workers منفصلة).

بدون التغيير ده، أي اختبار تحمل هنعمله دلوقتي هيوضّح لك بالأرقام حجم المشكلة، لكن الإصلاح الجذري مش في الكود — في مكان الاستضافة.

---

## الجزء 3: خطة الاختبار الوظيفي (قبل أي اختبار تحمل)

اختبري كل ده يدويًا (أو اطلبي من حد يساعدك) **قبل** ما تبدأي اختبار التحمل، عشان تتأكدي إن الأخطاء اللي هتظهر تحت الضغط هي أخطاء "حمل" مش أخطاء وظيفية أصلًا موجودة:

### تسجيل الدخول والحسابات
- [ ] تسجيل حساب جديد (تجربة مجانية تبدأ صح؟)
- [ ] تسجيل الدخول بحساب موجود
- [ ] نسيان كلمة المرور واستعادتها
- [ ] تسجيل الخروج من أكتر من صفحة

### الطلاب
- [ ] إضافة طالب جديد بكل الحقول
- [ ] تعديل بيانات طالب
- [ ] حذف طالب (وتأكيد إنه اختفى من كل الأماكن: لوحة التحكم، الجدول، الحصص)
- [ ] البحث عن طالب من الهيدر

### الجدول والمواعيد
- [ ] إضافة موعد أسبوعي عادي
- [ ] إضافة موعد **يومي** (تأكدي إنه ظهر في **كل الـ7 أيام** فعلًا بعد إصلاح اليوم)
- [ ] إضافة موعد شهري / بدون تكرار (بالتاريخ)
- [ ] تعديل موعد موجود
- [ ] حذف موعد واحد
- [ ] حذف "كل أيام التكرار" لموعد يومي
- [ ] محاولة إضافة موعد بيتعارض مع موعد موجود (المفروض تظهر رسالة تعارض واضحة)

### الحصص
- [ ] الضغط على "بدء الحصة" من صفحة الطالب → يوديكي لصفحة الحصة
- [ ] الضغط على "بدء الحصة" في صفحة الحصة نفسها → يشتغل العداد فعلًا
- [ ] إنهاء الحصة بحفظ كل الحقول
- [ ] التأكد إن الحصة ظهرت في سجل الطالب بعد الحفظ

### الاشتراك
- [ ] بانر الفترة التجريبية بيعرض العدد الصحيح للأيام
- [ ] زر الاشتراك بيفتح واتساب بالرقم والرسالة الصح
- [ ] (لو ممكن تجربيها) تغيير حالة اشتراك تجريبي لـ"منتهي" يدويًا في Supabase وتتأكدي إن **شاشة القفل** بتظهر فعلًا وبتوريلك عدد طلابك الحقيقي

### الموبايل تحديدًا
- [ ] الشريط السفلي بيشتغل ويودي للصفحة الصح
- [ ] حصص اليوم بتتعرض بشكل مرتب عمودي
- [ ] فتح نافذة إضافة موعد على شاشة صغيرة (مفيش عناصر متقطعة)

---

## الجزء 4: خطة اختبار التحمل بالتفصيل (خطوة بخطوة)

### الأداة المقترحة: **k6** (مجانية ومفتوحة المصدر، مصممة تحديدًا لاختبار APIs زي بتاعتك)

#### الخطوة 1: التثبيت
على جهازك (Windows/Mac/Linux) أو أي سيرفر تجربة:
```bash
# Windows (باستخدام winget)
winget install k6 --source winget

# Mac
brew install k6

# Linux (Debian/Ubuntu)
sudo apt install k6
```

#### الخطوة 2: اختاري endpoint "قراءة فقط" آمن للاختبار
**لا تختبري أبدًا بحمل عالي على endpoints بتكتب في قاعدة البيانات** (زي `create-schedule` أو `create-student`) لأنك هتملي الداتابيز ببيانات وهمية آلاف المرات. ابدئي بـ:
- `/check-subscription` (GET-like، قراءة فقط)
- `/get-schedules` (قراءة فقط)
- `/get-dashboard` (قراءة فقط)

دول أفضل مؤشر لأنهم هيتنادوا فعليًا في كل مرة أي مستخدمة تفتح صفحة.

#### الخطوة 3: سكريبت k6 جاهز (عدّلي التوكن بتاعك)

احفظي الملف ده باسم `load-test.js`:

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';

// ⚠️ حطي هنا توكن حقيقي لمستخدمة تجربة (مش حساب أساسي!)
// تقدري تجيبيه من: افتحي البرنامج في المتصفح -> Developer Tools -> Application -> Local Storage
// أو من الـ Network tab وانتِ بتفتحي أي صفحة، دور على Authorization header
const AUTH_TOKEN = 'ضعي_التوكن_هنا';
const BASE_URL = 'https://tasneemahmed-n8n.hf.space/webhook';

export const options = {
  stages: [
    { duration: '30s', target: 10 },   // تدرج لـ 10 مستخدمين
    { duration: '30s', target: 50 },   // بعدين 50
    { duration: '30s', target: 100 },  // بعدين 100
    { duration: '1m',  target: 300 },  // الهدف: 300 متزامن
    { duration: '30s', target: 0 },    // تهدئة تدريجية
  ],
  thresholds: {
    http_req_duration: ['p(95)<3000'], // 95% من الطلبات لازم تكون أقل من 3 ثواني
    http_req_failed: ['rate<0.05'],    // نسبة الفشل لازم تكون أقل من 5%
  },
};

export default function () {
  const headers = {
    'Authorization': `Bearer ${AUTH_TOKEN}`,
    'Content-Type': 'application/json',
  };

  const res = http.get(`${BASE_URL}/check-subscription`, { headers });

  check(res, {
    'status is 200': (r) => r.status === 200,
    'response has success field': (r) => {
      try { return JSON.parse(r.body).success === true; } catch (e) { return false; }
    },
  });

  sleep(1); // محاكاة استخدام واقعي (مش قصف مستمر بدون توقف)
}
```

#### الخطوة 4: تشغيل الاختبار
```bash
k6 run load-test.js
```

#### الخطوة 5: قراءة النتائج
هتشوفي جدول في الآخر فيه أرقام زي:
```
http_req_duration..............: avg=xxx ms  p(95)=xxx ms
http_req_failed.................: x.xx%
http_reqs.......................: xxxx requests
```

**إزاي تفهمي الأرقام:**

| المؤشر | ✅ كويس | ⚠️ محتاج انتباه | ❌ خطر حقيقي |
|---|---|---|---|
| `p(95)` (95% من الطلبات) | أقل من 1-2 ثانية | 2-5 ثواني | أكتر من 5 ثواني |
| `http_req_failed` (نسبة الفشل) | أقل من 1% | 1-5% | أكتر من 5% |
| الاستقرار مع زيادة الحمل | زمن الاستجابة بيزيد بالتدريج بشكل منطقي | بيقفز فجأة عند رقم معيّن (يعني وصلتِ لحد أقصى) | طلبات بتفشل بالكامل (timeout / 500 / 502) |

### الخطوة 6: راقبي n8n وانتِ بتشغلي الاختبار
افتحي لوحة تحكم Hugging Face Space بتاعتك في تاب تاني وانتِ الاختبار شغال، وشوفي:
- استهلاك الـ CPU وصل لـ 100% ولا لسه فاضي؟
- الـ RAM قربت تخلص؟
- فيه رسائل "Space is restarting" أو أي إشارة إنه بيعيد تشغيل نفسه من الضغط؟

### الخطوة 7: كرري الاختبار على endpoint بيكتب (بحذر شديد)
لو عايزة تجربي `create-schedule` تحديدًا، اعملي كده على **بيئة تجربة منفصلة** (مش قاعدة البيانات الحقيقية بتاعت عملاء فعليين)، وخففي عدد المستخدمين لحوالي 20-30 بس، لأن كل طلب بيعمل INSERT فعلي.

---

## الجزء 5: خطة إطلاق آمنة (بدل الدخول المباشر بـ 300)

بدل ما تفتحي البرنامج لـ300 شخص مرة واحدة، أنصح بخطة تدريجية:

1. **أسبوع 1**: 20-30 مستخدمة فعليات، راقبي الأداء يوميًا.
2. **أسبوع 2**: لو مفيش شكاوى، زودي لـ80-100.
3. **أسبوع 3-4**: زودي تدريجيًا لحد 300.

الطريقة دي بتديكي وقت تكتشفي فيه أي مشكلة أداء **قبل** ما تأثر على عدد كبير من العملاء دفعة واحدة، وهي أرخص وأأمان بكتير من اختبار تحمل صناعي وحده.

---

## قائمة نهائية قبل الإطلاق

- [ ] نقل n8n من Hugging Face Space لاستضافة إنتاجية حقيقية (الأهم)
- [ ] تشغيل اختبار k6 وتسجيل النتائج (p95, نسبة الفشل) قبل وبعد نقل الاستضافة للمقارنة
- [ ] التأكد من وجود indexes على أعمدة `teacher_id`, `student_id`, `day_of_week` في جداول Postgres (لو مش موجودة، أي جدول بيكبر هيبطّئ الاستعلامات مع الوقت)
- [ ] مراجعة كل الـ checklist الوظيفي في الجزء 3
- [ ] خطة إطلاق تدريجي زي الجزء 5
- [ ] عمل نسخة احتياطية (backup) من قاعدة بيانات Supabase قبل أي اختبار تحمل يلمس بيانات حقيقية
