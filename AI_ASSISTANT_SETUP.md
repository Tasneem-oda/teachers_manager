# مساعد الذكاء الاصطناعي (شات) — دليل التفعيل والشرح الكامل

ميزة جديدة: زرار "مساعد الذكاء الاصطناعي" في صفحة الحصة (`lesson.html`)
بجانب "بدء الحصة"، بيفتح شات تفاعلي بذاكرة خاصة بكل طالب، مربوط بـ
Gemini (مجانًا من Google AI Studio) عن طريق n8n.

## الخطوات المطلوبة منك

### 1) شغّلي الـ migration
افتحي `n8n/AI_ASSISTANT_DB_MIGRATION.sql` وشغّليه مرة واحدة في Supabase
SQL Editor. بيعمل 3 حاجات:
- يضيف عمود `current_surah` لجدول `students` (كان ناقص فعليًا رغم إن
  الكود بيستخدمه - كان بيتجاهل بصمت عند الحفظ، شوفي التفاصيل في تعليق
  أول سطر بالملف)
- يضيف جدول `ai_chat_messages` (ذاكرة الشات لكل طالب)
- يضيف جدولين صغيرين لإدارة حدود الاستخدام (`ai_usage_daily`, `ai_rate_limiter`)

### 2) احصلي على مفتاح Gemini API مجاني
1. افتحي https://aistudio.google.com/apikey وسجّلي دخول بحساب Google
2. اعملي "Create API Key" (مش محتاجة بطاقة ائتمان للحصة المجانية)
3. انسخي المفتاح

### 3) اعملي Credential في n8n
Credential جديد من نوع **Header Auth**:
- Name: `Gemini API Key`
- Header Name: `x-goog-api-key`
- Header Value: المفتاح اللي نسختيه

### 4) استوردي الـ workflow
1. استوردي `n8n/ai-chat-assistant.json` في n8n
2. عقدة **Postgres** (كل عُقد الاستعلامات) المفروض تترابط تلقائيًا بنفس
   `Postgres account` credential المستخدم في باقي الـ workflows عندك
3. افتحي عقدة **"Call Gemini API"** واختاري الـ Credential الجديد
   `Gemini API Key` اللي عملتيه في الخطوة 3 (لازم تختاريه يدويًا)
4. فعّلي الـ workflow (Active)

### 5) اضبطي الحدود حسب حصتك الفعلية (اختياري لكن مهم)
افتحي عقدة **"Decode JWT & Config"** في بداية الـ workflow، هتلاقي كل
الإعدادات القابلة للتعديل في مكان واحد مع شرح كل واحدة:

| الإعداد | الافتراضي | الوظيفة |
|---|---|---|
| `dailyLimit` | 40 | أقصى عدد طلبات لكل معلّم في اليوم |
| `minIntervalSeconds` | 5 | أقل فاصل بالثواني بين أي طلبين في النظام كله (حماية من تجاوز حد الدقيقة عند Google) |
| `maxMessageLength` | 600 | أقصى طول لرسالة المعلم |
| `memoryLimit` | 20 | عدد الرسائل المحفوظة في ذاكرة كل طالب (~10 تبادلات) |
| `geminiModel` | `gemini-2.0-flash-lite` | اسم الموديل المستخدم |

**مهم:** حدود Google للحصة المجانية (عدد الطلبات بالدقيقة/باليوم) بقت
بتتغيّر وبتختلف حسب حسابك بالتحديد، ومفيش رقم ثابت واحد ينفع لكل
الحسابات. تابعي حدودك الفعلية من https://aistudio.google.com/usage
وعدّلي `minIntervalSeconds` و`dailyLimit` فوق حسبها. لو بدأتي تشوفي
رسائل "الخدمة مزدحمة" كتير، كبّري `minIntervalSeconds`.

## إزاي الميزة شغالة

### من ناحية المعلم
1. تفتح صفحة الحصة، تدوس على زرار "مساعد الذكاء الاصطناعي" (بجانب "بدء الحصة")
2. شات بسيط بيفتح، تكتب طلبها بشكل طبيعي، مثلاً:
   - "اشرح من آية ٥ لآية ١٠"
   - "اشرح الصفحة التالية للطالب"
   - "اقترح طريقة أشرح بيها الدرس ده بسهولة"
3. المساعد بيرد مع مراعاة: موضع الطالب الحالي في الحفظ، سجل آخر 5 حصص،
   وملاحظات المعلم المسجّلة عن الطالب (لو فيها إشارة إنه بيحب الأمثلة
   العملية أو القصص، المساعد بيتصرف على أساسها تلقائيًا من غير ما يُسأل)
4. تحت الشات بيظهر عدد الطلبات المتبقية اليوم

### الذاكرة
- كل رسالة (من المعلم والمساعد) بتتسجّل في جدول `ai_chat_messages` مربوطة بالطالب
- مع كل رسالة جديدة، آخر 20 رسالة (10 تبادلات) بترجع تلقائيًا كسياق
  للمحادثة - المساعد "فاكر" آخر كلام اتقال بالظبط زي ما طلبتي
- الذاكرة دي خاصة بكل طالب لوحده (مش مشتركة بين الطلاب)

### حماية الحصة المجانية من الأخطاء
- **حد يومي لكل معلّم** (`dailyLimit`): لو معلّم وصل للحد، بيظهرله رسالة
  واضحة ("وصلتِ للحد الأقصى...") بدون ما يأثر على باقي المعلمين
- **حد عام بين كل الطلبات** (`minIntervalSeconds`): لو طلبين جم قريبين
  من بعض من أي معلمين، التاني بياخد رسالة "النظام مشغول لحظة" ويقدر
  يعيد المحاولة فورًا تقريبًا
- **لو Gemini نفسه رجّع خطأ** (ازدحام مؤقت 429، أو رفض الرد لأسباب أمان
  المحتوى)، المعلم بياخد رسالة واضحة بالعربي، **وما بيتخصمش من رصيدها
  اليومي** (بيترجع تلقائيًا - "Rollback")
- كل الحدود دي atomic على مستوى قاعدة البيانات، يعني حتى لو معلمين
  بعتوا في نفس اللحظة بالظبط، الحساب بيفضل صحيح

## ملف الملاحظة الإضافية
لاحظنا أثناء بناء الميزة دي إن عمود `current_surah` كان مستخدم في الكود
الحالي (`js/students.js`, `js/student.js`, `js/dashboard.js`) بس مش
موجود في قاعدة بياناتك الفعلية - فكانت بتُكتب في الفورم وتتفقد بصمت.
الـ migration فوق بيضيفه، وده بيصلح المشكلة القديمة دي تلقائيًا كـ"هدية"
جانبية، من غير أي تعديل على أي كود موجود.
