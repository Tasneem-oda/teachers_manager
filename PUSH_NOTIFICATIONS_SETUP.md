# إشعارات Push اليومية بحصص المعلم — دليل التفعيل

هذه الميزة بترسل إشعار يوميًا لكل معلّم بحصصه المجدولة النهاردة (حسب
جدوله في صفحة "المواعيد")، حتى لو التطبيق مقفول أو الموبايل في وضع
السكون. الميزة مبنية على خدمة **OneSignal** (مجانية لحجم استخدام كبير)
عشان نتفادى تعقيد إدارة مفاتيح VAPID وService Worker خاص بالإشعارات يدويًا.

## ⚠️ إصلاح مهم: سبب عدم ظهور الإشعار اليومي فعليًا على الشاشة

لو الإشعارات كانت شغالة في الإعداد (App ID مضبوط، الـ workflow مفعّل في
n8n) لكن **الإشعار اليومي مش بيظهر فعليًا على شاشة الموبايل غير لو
التطبيق مفتوح وقتها بالظبط** (وده بيتضح أكتر مع المستخدمين المثبّتين
التطبيق على شاشة الهاتف)، السبب كان **تعارض بين ملفين Service Worker**
في الكود، مش مشكلة في إعداد OneSignal نفسه:

- `sw.js` (المطلوب لتثبيت التطبيق PWA) كان بيتسجّل من `js/pwa-install.js`
  في كل تحميل صفحة، وبيفرض سيطرته فورًا (`skipWaiting` + `clients.claim`).
- `OneSignalSDKWorker.js` (اللي فيه فعليًا معالج استقبال الإشعار) كان
  بيتسجّل تلقائيًا بواسطة SDK بتاع OneSignal، على نفس الـ scope بالظبط.

المتصفح مش بيسمح بأكتر من Service Worker نشط واحد لكل scope، فكل مرة
المعلم يفتح التطبيق، `sw.js` كان بيستبدل تسجيل OneSignal ويلغي اشتراك
الإشعارات بصمت من غير أي خطأ ظاهر - فـ n8n كان "يبعت" الإشعار فعليًا
(وده كان بيظهر Delivered في لوحة تحكم OneSignal)، لكن مفيش أي Service
Worker شغال عنده معالج استقبال ليعرضه على الشاشة.

**تم إصلاح المشكلة دي في الكود** بدمج كود OneSignal جوه نفس ملف `sw.js`
(عن طريق `importScripts`) بدل ما يكون في ملف منفصل، مع تعديل
`js/notifications.js` عشان يقول لـ OneSignal يستخدم `sw.js` نفسه بدل
ما يسجّل ملفه الخاص. كده بقى فيه تسجيل واحد بس لنفس الملف ونفس الـ
scope - مفيش تعارض ولا استبدال. **الإصلاح ده في الكود بالكامل ومفيش أي
خطوة إضافية مطلوبة منك غير رفع/نشر النسخة الجديدة من الموقع.** لكن
المستخدمين اللي عندهم تسجيل قديم متأثر بالمشكلة (خصوصًا المثبّتين
التطبيق) ممكن يحتاجوا يقفلوا التطبيق تمامًا ويفتحوه تاني مرة واحدة بعد
تحديث الموقع عشان المتصفح يسجّل الـ Service Worker الجديد المدمج.

## الخطوات المطلوبة منك (لازم تتعملوا قبل ما الميزة تشتغل فعليًا)

### 1) إنشاء حساب OneSignal وتطبيق Web Push
1. افتحي https://onesignal.com وسجّلي حساب مجاني.
2. من "New App/Website" اختاري **Web Push** واسمي التطبيق (مثلاً Teachers Manager).
3. لما يطلب نوع الموقع، اختاري **Typical Site** وحطي الدومين: `https://teachers-manager.online`
4. بعد الإنشاء، من `Settings → Keys & IDs` انسخي:
   - **OneSignal App ID**
   - **REST API Key**

### 2) ربط الـ App ID بالفرونت إند
افتحي `js/config.js` وحطي الـ App ID بدل القيمة الافتراضية:
```js
PUSH_NOTIFICATIONS: {
    ONESIGNAL_APP_ID: 'ضعي الـ App ID هنا'
},
```

### 3) ربط الـ App ID و REST API Key بـ n8n
1. استوردي الملف `n8n/send-daily-lesson-notifications.json` في n8n (Import from File).
2. افتحي عقدة **"Send OneSignal Notification"**، وفي حقل الـ Body استبدلي
   النص `REPLACE_WITH_YOUR_ONESIGNAL_APP_ID` بنفس الـ App ID اللي استخدمتيه
   في الخطوة 2.
3. في نفس العقدة، اعملي Credential جديد من نوع **Header Auth** (لو لسه
   مش موجود) بالبيانات دي:
   - Name: `OneSignal REST API Key`
   - Header Name: `Authorization`
   - Header Value: `Basic ضعي_الـ_REST_API_Key_هنا`
   ثم اختاريه في خانة الـ Credential بالعقدة (لازم تختاريه يدويًا لأن
   الاستيراد مش بيربط الـ credentials تلقائيًا لأسباب أمان).
4. عقدة **"Get Today Schedules Per Teacher"** بتستخدم نفس Postgres
   credential المستخدم في باقي الـ workflows عندك (`Postgres account`)،
   المفروض يترابط تلقائيًا لأنه بنفس الـ ID، لو ظهرت علامة تحذير أعيدي
   اختياره يدويًا.
5. فعّلي الـ workflow (زرار Active أعلى الصفحة).

الـ workflow مضبوط يشتغل يوميًا الساعة **7:00 صباحًا بتوقيت القاهرة**
(cron: `0 7 * * *`). تقدري تغيّري الوقت من عقدة "كل يوم الساعة 7 صباحًا".

### 4) لا حاجة لأي migration في قاعدة البيانات
الميزة دي **ما بتضيفش أي جدول جديد** في Postgres — OneSignal نفسها بتخزن
وتدير اشتراكات الإشعارات، وبتربطها بمعرّف المعلم (`external_id`) اللي
هو نفس الـ UUID بتاعه في Supabase. الاستعلام بيقرأ بس من جدول
`teachers_manager.schedules` الموجود بالفعل.

## إزاي الميزة بتشتغل من ناحية المستخدم
1. لما المعلم يسجّل دخوله، الجهاز بيتربط تلقائيًا بحسابه (`OneSignal.login`).
2. بعد ثانيتين من تحميل أي صفحة رئيسية، بيظهر بانر بسيط تحت الشاشة
   يسأله "فعّل تذكير الحصص اليومي" — لو ضغط "تفعيل الإشعارات"، المتصفح
   بيطلب الإذن الرسمي.
3. تقدر كمان تفعّلي الإشعارات في أي وقت من زرار الجرس 🔔 في الهيدر العلوي.
4. كل يوم الساعة 7 صباحًا، workflow n8n بيجيب كل معلّم عنده حصة واحدة
   على الأقل اليوم (بناءً على `day_of_week` ونوع التكرار: أسبوعي/يومي/
   شهري/مرة واحدة — بنفس منطق صفحة الجدول تمامًا)، ويبعتله إشعار فيه
   عدد الحصص وأول 5 حصص بالاسم والوقت.
5. الضغط على الإشعار بيفتح صفحة `schedule.html` مباشرة.
6. المعلم اللي مفيش عنده حصص اليوم، أو لسه مفعّلش الإذن، مش هيوصله إشعار.

## ملاحظات مهمة
- الموقع لازم يفضل شغال على HTTPS (متحقق بالفعل، منشور على دومين حقيقي).
- الحد المجاني في OneSignal بيغطي عدد مستخدمين كبير جدًا، فمش المفروض
  تحتاجي خطة مدفوعة في المدى القريب.
- لو معلّم سجّل دخول على أكتر من جهاز (موبايل + كمبيوتر)، OneSignal
  بتبعت الإشعار لكل الأجهزة المرتبطة بحسابه تلقائيًا.
- عند تسجيل الخروج، الكود بيفك ربط الجهاز عن هوية المعلم (`OneSignal.logout()`)
  عشان لو الجهاز مشترك بين أكتر من معلّم منبعتش إشعارات غلط.

---

## مركز الإشعارات (نافذة زر الجرس 🔔) — ميزة جديدة

بالإضافة لإشعار الـ Push، دلوقتي فيه **نافذة إشعارات داخل التطبيق نفسه**
تظهر عند الضغط على زر الجرس 🔔 في أعلى أي صفحة (شغالة على نسخة الويب
والموبايل، وكمان على التطبيق المثبّت على الشاشة الرئيسية - كلهم نفس
الكود بالظبط). النافذة فيها تبويبين:

- **غير مقروءة**: الإشعارات الجديدة اللي المعلم لسه مفتحهاش، مع عدد
  الإشعارات الجديدة ظاهر جنب اسم التبويب، ونقطة حمراء 🔴 فوق زرار
  الجرس نفسه لو فيه إشعارات جديدة (حتى قبل ما يفتح النافذة).
- **مقروءة**: الإشعارات اللي المعلم فتحها/ضغط عليها قبل كده، وبتتمسح
  تلقائيًا من قاعدة البيانات بعد **30 يوم** من تاريخ تحديدها كمقروءة
  (مفيش داعي لأي تدخل يدوي).

الضغط على أي إشعار في القائمة بيحدده كمقروء تلقائيًا (وينده على الصفحة
المرتبطة بيه لو موجودة، زي `schedule.html`)، وفيه كمان زرار "تحديد الكل
كمقروء" أعلى القائمة.

### خطوات التفعيل (منفصلة تمامًا عن إعداد OneSignal فوق)

1. **افتحي محرر SQL في Supabase** ونفّذي محتوى الملف
   `n8n/NOTIFICATIONS_DB_MIGRATION.sql` مرة واحدة (بينشئ جدول
   `teachers_manager.notifications`).
2. **استوردي في n8n** الملفين الجديدين:
   - `n8n/get-notifications.json` (يرجّع قائمة إشعارات المعلم).
   - `n8n/mark-notifications-read.json` (يحدد إشعار واحد أو كل
     الإشعارات كمقروءة).
   في كل واحد منهم، تأكدي إن عقد الـ Postgres مربوطة بنفس
   `Postgres account` credential المستخدم في باقي الـ workflows (زي ما
   حصل مع `get-schedules`)، وفعّلي الـ workflow (زرار Active).
3. **أعيدي استيراد `n8n/send-daily-lesson-notifications.json`** (اتعدّل
   عشان يحفظ نسخة من كل إشعار يومي في جدول `notifications` كمان، ويمسح
   الإشعارات المقروءة اللي عدّت عليها 30 يوم تلقائيًا كل يوم الساعة 7
   صباحًا). بعد الاستيراد، تأكدي إن:
   - عقدة "Send OneSignal Notification" لسه فيها الـ App ID الصحيح
     والـ credential بتاع REST API Key (زي الخطوة 3 فوق بالظبط - أحيانًا
     إعادة الاستيراد بتفضي إعدادات الـ credentials فبيتطلب اختيارها يدويًا
     تاني).
   - كل عقد الـ Postgres الجديدة (`Insert Notification Record` و
     `Cleanup Old Read Notifications`) مربوطة بنفس `Postgres account`.
4. مفيش حاجة تانية - النافذة نفسها هتشتغل تلقائيًا من `js/notifications.js`
   و`js/sidebar.js` (زرار الجرس موجود بالفعل في كل صفحة).

### ملاحظات
- مركز الإشعارات ده **مستقل تمامًا عن إذن الـ Push**: بيشتغل حتى لو
  المعلم رفض إذن إشعارات المتصفح، لأنه بس بيعرض سجل مخزّن عندنا في
  قاعدة البيانات، مش مرتبط بإذن المتصفح.
- لو حد الميزتين (الـ workflows الجديدة) لسه مش متفعّل، الكود بيفشل
  بصمت (رسالة تحذير في الـ console بس) من غير ما يكسر أي صفحة تانية في
  البرنامج.
