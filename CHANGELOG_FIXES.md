# سجل الإصلاحات — النسخة المصححة بالكامل

تم فحص كل ملف في المشروع (13 صفحة HTML، 6 ملفات JS، ملف CSS واحد) وحل كل التعارضات والأخطاء التالية:

## 1) خطأ حرج كان يعطّل التطبيق بالكامل
الصفحات التالية كانت **تنهار عند فتحها مباشرة** (شاشة بيضاء + خطأ في الـ console: `Cannot read properties of undefined (reading 'createClient')`)، لأنها كانت تستورد `auth.js`/`api.js` بدون تحميل مكتبة Supabase (`<script src=".../supabase-js@2">`) ولا ملف `config.js` أولاً:
- `dashboard.html`
- `students.html`
- `student.html`
- `schedule.html`
- `books.html`

✅ تمت إضافة السكريبتات الناقصة لكل صفحة من هذه الصفحات الخمس، بنفس الترتيب المستخدم في الصفحات التي كانت تعمل بالفعل (`login.html`, `settings.html`, ...).

## 2) ملفات JS ميتة/متضاربة
- `js/students.js`, `js/dashboard.js`, `js/student.js` — لم تكن مُستدعاة من أي صفحة إطلاقًا، وتحتوي على منطق قديم يشير لعناصر HTML لم تعد موجودة (بقايا نسخة سابقة من المشروع). **تم حذفها بالكامل** لمنع أي التباس مستقبلي.
- تنظيف `js/auth.js`: كان يُصدّر `supabase` كقيمة ثابتة وقت تحميل الوحدة (قبل ضمان جاهزية `window.supabaseClient`)، وهو غير مستخدم في أي مكان — تم توضيح الكود بدلاً من ذلك.

## 3) كلاسات CSS مفقودة كانت تسبب تصميمًا مكسورًا
`.modal`, `.modal-content`, `.close-modal`, `.loading`, `.loader`, `.empty-state` كانت مستخدمة في عدة صفحات لكنها معرّفة محليًا في بعضها فقط — وغير معرّفة إطلاقًا في صفحات أخرى (مثل نافذة إضافة الموعد في `schedule.html`، وحالة التحميل في `dashboard.html`)، مما يجعلها تظهر بدون أي تنسيق.
✅ تم توحيدها في `css/style.css` وحذف كل التكرارات المحلية المتضاربة.

## 4) الخط العربي لم يكن يُحمَّل إطلاقًا
كل الصفحات تستخدم `font-family: 'IBM Plex Sans Arabic'` لكن لا يوجد أي رابط لتحميل هذا الخط من Google Fonts في أي مكان — فكان يظهر فعليًا بخط Tahoma الافتراضي.
✅ تمت إضافة `@import` لخط IBM Plex Sans Arabic في `style.css` (تطبيق واحد يغطي كل الصفحات).

## 5) كتل `:root` متكررة ومتضاربة
8 صفحات كانت تعيد تعريف متغيرات CSS بأسماء غير مستخدمة (`--bg`, --text`, `--card`) تتعارض اسميًا مع نظام المتغيرات الفعلي (`--bg-primary`, `--text-primary`, `--card-bg`) المعرّف مركزيًا. **تم حذفها من كل الصفحات.**

## 6) تصميم الأيقونات
تم استبدال كل الأيقونات الرمزية (الإيموجي) بمكتبة أيقونات SVG موحّدة (`js/icons.js`) بنفس أسلوب الخطوط النظيفة الموجود في الصور المرجعية، مع الحفاظ على نفس ألوان الهوية (البنفسجي الغامق، الذهبي، الأخضر) المستخدمة أصلًا في `style.css`. كما تمت إضافة جرس الإشعارات في الهيدر العلوي مطابقةً للتصميم المرجعي.

## 7) تنظيفات إضافية
- إزالة مستمع حدث (`event listener`) فارغ بلا فائدة في `students.html`.
- توحيد استخدام كلاس `.close-modal` بين زر الإغلاق الرمزي (×) وزر "إلغاء" النصي لمنع تعارض الأنماط بينهما.
- إزالة كلاسات `.navbar` القديمة غير المستخدمة في الصفحات التي تعتمد النظام الجديد (`sidebar` + `top-header`).

## التحقق
- تم فحص جميع ملفات `js/*.js` والسكريبتات المضمّنة داخل كل صفحات HTML الـ13 بأداة `node --check` — لا توجد أي أخطاء صياغية (Syntax Errors).
- تم التأكد من توازن الوسوم (`<div>`/`</div>`) وتوازن الأقواس `{ }` في CSS.
- تم التأكد من عدم وجود أي إشارة متبقية للملفات المحذوفة.

---

# تحديث ثانٍ: خطاب محايد + تصميم موبايل مصحّح بالكامل

## 1) الخطاب أصبح يشمل الذكر والأنثى (وليس المعلمة فقط)
تم فحص كل نص في المشروع (13 صفحة HTML + 6 ملفات JS) وتحديد كل الصيغ التي كانت تخاطب المعلمة فقط بصيغة المؤنث (فعل أمر مؤنث، أو كلمة "معلمة")، وتم تعديلها لصيغة محايدة/مذكرة معتادة في واجهات البرمجيات العربية:
- `subscription.html`: "اختاري الخطة المناسبة لكِ" ← "اختر الخطة المناسبة لك"
- `dashboard.html`: "افتحي ملف أي طالب..." ← "افتح ملف أي طالب..."
- `student.html`: "سجّلي أي ملاحظة..." ← "سجّل أي ملاحظة..."
- `lesson.html`: "سجّلي تفاصيل الحصة وأنهيها..." ← "سجّل تفاصيل الحصة وأنهها..."
- `schedule.html`: "نظّمي كل مواعيدك..." ← "نظّم كل مواعيدك..."، و"اضغطي للإضافة" ← "اضغط للإضافة"
- `books.html`: الفقرة العامية بالكامل ("هتقدري ترفعي... وتعرضيها") ← صيغة محايدة ("هتقدر ترفع... وتعرضها")
- `js/sidebar.js`: تسمية الدور الافتراضية "معلمة" (مرتين) ← "معلم"
- `js/auth.js`: رسالة الخطأ "جرّبي تسجيل الدخول... تواصلي معنا" ← "جرّب تسجيل الدخول... تواصل معنا"
- `subscription.html`: "ثم ترقي كلما احتجت" (صيغة مؤنثة غير واضحة) ← أعيدت صياغتها بالكامل: "ثم قم بالترقية كلما احتجت" (صيغة محايدة لا لبس فيها)

تم أيضًا التأكد من أن الكلمات الأخرى في الموقع (مثل "طلابك"، "بياناتك"، "جدولك") محايدة أصلًا في الكتابة العربية بدون تشكيل، فلم تكن بحاجة لتعديل.

## 2) تصميم الموبايل — إعادة بناء كاملة
كانت مشكلة الموبايل الأساسية أن السايدبار كان يتحول عند تصغير الشاشة إلى شريط علوي أفقي قابل للتمرير بعرض كامل الشاشة — وهو تصميم غير مطابق للصورة المرجعية (تطبيق الموبايل الفعلي) وغير عملي. تم استبداله بالكامل:

- **شريط تنقل سفلي ثابت (Bottom Tab Bar)** يطابق تصميم صور الموبايل المرجعية: أيقونة في الأعلى + تسمية قصيرة أسفلها لكل صفحة (الرئيسية / الطلاب / المواعيد / الملاحظات / كتابي / الإعدادات)، ثابت أسفل الشاشة مع مراعاة `safe-area-inset` لأجهزة آيفون ذات الحافة السفلية.
- تمت إضافة `padding-bottom` كافٍ للمحتوى الرئيسي حتى لا يختفي أي جزء من المحتوى خلف الشريط السفلي الثابت.
- **زر تسجيل خروج مخصص للموبايل** أُضيف في الهيدر العلوي (بجانب جرس الإشعارات) لأن رابط "تسجيل الخروج" العادي في أسفل السايدبار يختفي على الموبايل ضمن الشريط الجديد — وبهذا لا يفقد مستخدم الموبايل القدرة على تسجيل الخروج (كانت هذه فجوة حقيقية في النسخة السابقة).
- تقليص الحشو (padding) الزائد في الهيدر العلوي ومحتوى الصفحة على الشاشات الصغيرة.
- إخفاء صندوق البحث تلقائيًا على الشاشات الأصغر من 480px لتفادي الازدحام، مع إبقائه بعرض مرن على الشاشات المتوسطة.
- إصلاح تراكب/فيضان محتمل: قوائم الطلاب (`students-grid`)، وشبكة خطط الاشتراك (`pricing-grid`) كانت تحدد عرضًا أدنى للعمود (300px/280px) قد يتجاوز عرض الشاشات الصغيرة جدًا (320px) ويسبب تمرير أفقي غير مقصود — تم تقليص القيم وإضافة عمود واحد على الشاشات الصغيرة.
- أزرار الإجراءات المتجاورة (مثل "إضافة موعد/بدء الحصة" في صفحة الطالب، أو "فتح رابط الحصة/بدء الحصة" في صفحة الحصة) أصبحت تصطف عموديًا بعرض كامل على الشاشات الضيقة بدل التزاحم أفقيًا.
- عناصر الحصة في لوحة التحكم (`lesson-item`) أصبحت تلتف بشكل صحيح على الشاشات الصغيرة بدل أن يتم ضغطها في سطر واحد.
- جدول لوحة التحكم الإدارية (`admin.html`) كان يستخدم `overflow: hidden` وهو ما كان يقصّ الأعمدة الإضافية بدل السماح بتمريرها — تم تغييره إلى `overflow-x: auto` مع تفعيل التمرير الأفقي للجدول، وتفعيل نفس المبدأ لتبويبات الصفحة.
- تصغير حجم العناوين الكبيرة (الصفحة الرئيسية/الصفحة التعريفية، وصفحة الحصة) على الشاشات الصغيرة حتى لا تكسر التخطيط.
- تقليص حشو نماذج تسجيل الدخول/التسجيل/استعادة كلمة المرور على الشاشات الصغيرة جدًا (أقل من 420px) للاستفادة القصوى من المساحة المتاحة.

## التحقق النهائي
- تم إعادة فحص جميع ملفات JS (المستقلة والمُضمّنة داخل كل صفحة) بأداة `node --check` — صفر أخطاء صياغية.
- تم التأكد من توازن وسوم `<div>` وتوازن أقواس `{ }` في كل من `style.css` وكل كتلة `<style>` محلية داخل الصفحات.
- تم إجراء بحث شامل (بعد إزالة علامات التشكيل) للتأكد من عدم تبقّي أي صيغة خطاب مؤنث حصري في أي ملف.

---

# تحديث ثالث: طلبات محددة (شريط سفلي، اشتراك، تكرار مواعيد، أزرار معطّلة، صيغة الوقت)

## 1) الشريط السفلي في الموبايل
- **مشكلة حقيقية تم اكتشافها وإصلاحها**: عنصرا التنقل "الطلاب" و"الملاحظات" كانا يشيران لنفس الصفحة تمامًا (`students.html`) — أي أيقونتين مختلفتين في الشريط السفلي تؤديان لنفس المكان بالضبط، وهو تصميم مربك في شريط تنقل سفلي (بخلاف السايدبار على الديسكتوب حيث كان أقل وضوحًا). تم حذف عنصر "الملاحظات" المكرر، وأصبح الشريط السفلي 5 عناصر فقط (الرئيسية / الطلاب / المواعيد / كتابي / الإعدادات) — وهو العدد المثالي القياسي لأشرطة التنقل السفلية في تطبيقات الموبايل. الملاحظات نفسها لا تزال متاحة بالكامل من داخل صفحة الطالب ومن الروابط السريعة بلوحة التحكم.
- تحسين حجم الأيقونات والتسميات في الشريط السفلي للاستفادة من المساحة الإضافية بعد تقليل العناصر لـ5.

## 2) إزالة "التجربة المجانية" من السايدبار ونقلها لمكان ثابت
تم حذف كارت "الخطة الاحترافية/التجربة المجانية" من السايدبار نهائيًا (كان يختفي بالكامل على الموبايل أصلًا، وهذا جزء من مشكلة تصميم الموبايل). بدلاً منه: بانر ثابت جديد يظهر في **أعلى لوحة التحكم مباشرة** (أول صفحة يراها المستخدم بعد الدخول) — يظهر فقط عند الحاجة الفعلية (فترة تجريبية أو منتهية)، ويختفي تلقائيًا عند التفعيل، ويعمل بنفس الشكل على الموبايل والديسكتوب.

## 3) حصص اليوم في الموبايل + تكديس العناصر عموديًا
- اكتُشفت المشكلة الجذرية: التقسيم الرئيسي للوحة التحكم (حصص اليوم + المساعد الذكي/الملاحظات) كان مكتوبًا بـ `style` مباشر بعمودين ثابتين (`2fr 1fr`) **خارج نظام الأنماط المتجاوب بالكامل**، فكان يبقى عمودين متجاورين حتى على أصغر الشاشات. تم نقله لكلاس `dashboard-grid` مع تكديسه لعمود واحد تحت 900px.
- تم إعادة تصميم كل عنصر ضمن "حصص اليوم" ليعرض: (صورة الطالب + الاسم) في سطر، ثم الوقت في سطر مستقل بعرض كامل، ثم زر "ابدأ الحصة" في سطر مستقل بعرض كامل — بدل التزاحم الأفقي السابق.

## 4) زر "بدء الحصة" في صفحة الحصة — لم يكن يعمل إطلاقًا
تم التأكد بالفحص المباشر للكود: الزر لم يكن مرتبطًا بأي دالة (`event listener`) على الإطلاق — بقايا من نسخة سابقة. الآن الزر يستدعي فعليًا `api.startLesson()`، يشغّل عداد وقت حي على الزر نفسه، يفتح رابط الحصة تلقائيًا في نافذة جديدة إن وُجد، ويستخدم `lesson_id` الصحيح الناتج عند حفظ الحصة لاحقًا (بدل استخدام `student_id` كمعرّف حصة بالخطأ كما كان يحدث سابقًا).

## 5) صفحة الاشتراك
أُعيدت كتابتها بالكامل: تمت إزالة "الخطة السنوية" وكل ما يخصها نهائيًا. الصفحة الآن تعرض فقط: النسخة التجريبية (معلوماتية) + **الخطة الشهرية بسعر 150 جنيه**، مع زر واحد "الاشتراك عبر واتساب" يفتح محادثة واتساب مباشرة لرقم **01037728764** برسالة جاهزة تتضمن اسم الخطة والسعر — بدل رسالة "سيتم تفعيل الدفع قريبًا" الوهمية السابقة.

## 6) تكرار المواعيد (يوميًا / أسبوعيًا / شهريًا / بدون تكرار)
- أُضيف حقل اختيار نوع التكرار في نافذة إضافة/تعديل الموعد بصفحة الجدول.
- **أسبوعيًا** (الافتراضي): نفس السلوك القديم تمامًا (يوم أسبوعي ثابت).
- **يوميًا**: يُنشئ الموعد تلقائيًا في **كل أيام الأسبوع السبعة** بنفس الوقت (7 مواعيد مرتبطة بمعرّف مجموعة واحد)، مع زر إضافي "حذف كل أيام التكرار" لحذفها كلها دفعة واحدة.
- **شهريًا / بدون تكرار**: يظهر منتقي تاريخ بدل اختيار اليوم، ويُحسب يوم الأسبوع المطابق تلقائيًا لعرضه في الجدول، مع شارة صغيرة ("شهريًا" / "مرة واحدة") على كتلة الموعد للتمييز.
- **تم تعديل ملف الأتمتة (n8n) المرفق فعليًا**: `n8n/create-schedule.json` — أصبح يستقبل ويخزّن حقول التكرار الجديدة.
- ⚠️ **يتطلب هذا التعديل خطوة يدوية واحدة قبل أن يعمل**: تنفيذ كود SQL المرفق في `n8n/REQUIRED_DB_MIGRATION.sql` داخل محرر SQL في Supabase (لإضافة 3 أعمدة جديدة لجدول `schedules`). كما يُنصح بتحديث استعلام SELECT في workflow "get-schedules" (غير مرفق في هذه الجلسة) لإرجاع الأعمدة الجديدة حتى تظهر شارات التكرار بشكل صحيح. النظام مصمم ليعمل بأمان بدون هذه الخطوة أيضًا (يتجاهل الحقول الناقصة بصمت)، لكن ميزة التكرار الفعلية تحتاج هذا التحديث لتُخزَّن بشكل صحيح.

## 7) صيغة الوقت 12 ساعة
تم تعديل `Formatters.formatTime` في `js/utils.js` ليعرض الوقت بصيغة 12 ساعة مع (ص/م) بدل صيغة 24 ساعة، وتطبيقها في كل موضع فعلي لعرض الوقت (لوحة التحكم، شبكة الجدول الأسبوعي وتسميات الساعات فيه). حقول إدخال الوقت (`<input type="time">`) تُركت كما هي عمدًا لأنها تتطلب صيغة 24 ساعة داخليًا حسب معيار HTML (المتصفح نفسه يعرضها للمستخدم بالصيغة المناسبة لجهازه تلقائيًا).

## 8) إصلاح إضافي لصيغة خطاب متبقية
تم العثور على وتصحيح صيغة مؤنثة حصرية إضافية لم تُكتشف في الجولة السابقة: `confirm('هل تريدين حذف هذا الموعد؟')` في `schedule.html` ← `'هل تريد حذف هذا الموعد؟'`.

## التحقق النهائي لهذا التحديث
- إعادة فحص جميع ملفات JS (مستقلة ومضمّنة) بـ `node --check` — صفر أخطاء صياغية.
- التأكد من توازن `<div>` وأقواس CSS في كل ملف مُعدَّل.
- التحقق من صحة ملف الـ n8n JSON المُعدَّل (`json.load` ناجح بدون أخطاء).
- بحث نهائي شامل تأكد من عدم تبقّي أي صيغة خطاب مؤنث حصري في أي مكان بالمشروع.

---

# تحديث رابع: تشخيص وإصلاح مشكلة "التكرار اليومي يظهر بيوم واحد فقط"

## التشخيص
تم فحص ملف `get-schedules.json` الذي أرسلته — **لا يوجد به أي خطأ**: الاستعلام يُرجع كل صفوف `schedules` الخاصة بالمعلم بدون أي قيد أو `LIMIT` يحصرها في يوم واحد، وباقي الـ workflow (فك تشفير التوكن، بناء الرد) سليم تمامًا.

**السبب الحقيقي كان في الفرونت إند** (`schedule.html`)، في الكود المسؤول عن إنشاء التكرار اليومي: كان يرسل 7 طلبات إنشاء موعد (لكل أيام الأسبوع) داخل حلقة `for` واحدة، بدون معالجة أخطاء لكل يوم على حدة. فلو حدث **تعارض حقيقي** (موعد آخر محجوز بالفعل في نفس التوقيت في أحد الأيام — مثل يوم الإثنين إذا كان هناك طالب آخر محجوز الساعة نفسها) عند أي يوم غير الأول، كانت الحلقة بأكملها تتوقف فورًا عند أول فشل — فتبقى فقط الأيام التي نجحت *قبل* الفشل (غالبًا يوم واحد فقط، وهو ما لاحظتِه بالضبط).

## الإصلاح
تم تعديل حلقة إنشاء التكرار اليومي في `schedule.html` بحيث:
- تحاول إنشاء الموعد في **كل الأيام السبعة** بغض النظر عن نجاح أو فشل أي يوم سابق (كل يوم له `try/catch` مستقل).
- في النهاية تعرض رسالة واضحة: عدد الأيام التي تم حفظها فعليًا، وأسماء الأيام التي بها تعارض ولم تُضف (مع سبب كل تعارض).
- لو فشلت كل الأيام السبعة، تظهر رسالة خطأ واضحة بدل فشل صامت.

بهذا، لن يعود التكرار اليومي "يظهر بيوم واحد فقط" بدون تفسير — إما يظهر في كل الأيام المتاحة فعلاً، أو تعرفين بالضبط أي يوم فيه تعارض ولماذا.

---

# تحديث خامس: عرض الأيام المتبقية بدقة + بوابة قفل حقيقية عند انتهاء الاشتراك

بناءً على شكل جدول `teachers_manager.subscriptions` الفعلي (`status`, `trial_ends_at`, `paid_until`) وملف `check-subscription.json` الذي تم إرساله:

## 1) حساب حالة الاشتراك أصبح مبنيًا على التواريخ الفعلية وليس فقط نص status
أُضيفت دالة مشتركة `computeSubscriptionState()` في `js/sidebar.js` تقارن `trial_ends_at`/`paid_until` بالوقت الحالي فعليًا بدل الاعتماد فقط على قيمة `status` النصية (التي تُضبط يدويًا وقد تصبح غير دقيقة لو نسيتِ تحديثها). أي حالة نصية غير `trial`/`active`/`none` (مثل `expired` أو `cancelled` لو استخدمتِها يدويًا لاحقًا) تُعتبر تلقائيًا "مقفلة" — وهذا يمنحك طريقة يدوية بسيطة لقفل أي حساب متى شئتِ (فقط غيّري قيمة status).

## 2) عرض "كام يوم متبقٍ" في أعلى صفحة الاشتراك (لكل الحالات)
- **تجريبي**: "لديك X متبقية من الفترة التجريبية" (وأُصلحت صياغة الجمع العربي لتكون صحيحة نحويًا: يوم واحد / يومان / X أيام / X يومًا).
- **مشترك (مدفوع)**: أصبح يعرض الآن أيضًا "متبقٍ X على نهايتها" بدل رسالة عامة بدون تاريخ — بالاعتماد على `paid_until`.
- **منتهي فعليًا** (تجريبي أو مدفوع): بانر أحمر برسالة مناسبة للسبب.
- تمت أيضًا إضافة تذكير تلقائي في لوحة التحكم عندما يتبقى 5 أيام أو أقل على نهاية الاشتراك المدفوع، لتفادي المفاجأة عند الانتهاء.

## 3) بوابة قفل حقيقية (Paywall) عند انتهاء الاشتراك فعليًا
عند انتهاء التجربة المجانية أو الاشتراك المدفوع فعليًا (حسب التواريخ الحقيقية)، تظهر الآن شاشة قفل كاملة فوق كل صفحات البرنامج (ما عدا صفحة الاشتراك نفسها، حتى يقدر المستخدم يشترك دائمًا) تحتوي على:
- توضيح واضح إن الفترة انتهت.
- **رسالة طمأنة صريحة ومخصصة** تذكر العدد الفعلي لطلاب المستخدم ("لديك X طالب... وكل حصصهم وملاحظاتهم موجودة كما هي، ولن يُحذف منها أي شيء") بدل رسالة عامة، لتأكيد فعلي أن البيانات لسه موجودة.
- زر "تجديد الاشتراك عبر واتساب" بنفس رقم وطريقة صفحة الاشتراك.
- رابط لعرض تفاصيل الاشتراك، وزر لتسجيل الخروج (حتى لا يكون المستخدم عالقًا بلا أي مخرج).

### ⚠️ حدود هذا الحل (مهم جدًا تعرفيه)
هذا القفل **مطبَّق في الواجهة الأمامية فقط** (JavaScript). يمنع فتح الصفحات واستخدامها بصريًا، لكنه **لا يمنع** استدعاءات الـ API مباشرة لو حد تقني حاول تجاوزه، لأن أي workflow في n8n حاليًا (create-schedule, get-schedules, start-lesson, إلخ) **لا يتحقق من حالة الاشتراك بنفسه** قبل التنفيذ. لو الحماية الحقيقية مهمة (مش بس تجربة استخدام)، الخطوة التالية المنطقية هي إضافة نفس التحقق (`Get Subscription` + شرط IF) داخل كل workflow حساس، تحت — ممكن أساعدك فيها لو حبيتِ.

---

# تحديث سادس: تحسينات أداء أولية + خطة اختبار تحمل لـ 300 مستخدم

## إصلاحات أداء في الكود
1. **إزالة الاستدعاء المزدوج لـ `/check-subscription`**: كانت لوحة التحكم تستدعيه مرتين منفصلتين (من بوابة القفل ومن بانر التجربة). أُضيفت دالة `getSubscriptionCached()` في `js/sidebar.js` تدمج الطلبات المتزامنة في طلب واحد، وتخزّن النتيجة في `sessionStorage` لمدة 3 دقائق لتقليل الاستعلامات المتكررة عند التنقل بين الصفحات.
2. **تحويل حلقة إنشاء الموعد اليومي من تتابعية إلى متوازية**: كانت 7 طلبات إنشاء (لكل أيام الأسبوع) تُرسل الواحد تلو الآخر في `schedule.html`؛ أصبحت تُرسل بالتوازي عبر `Promise.allSettled` بما أن كل يوم مستقل عن الآخر، ما يقلل زمن الانتظار من 7 أضعاف إلى ما يقارب طلبًا واحدًا.

## اكتشاف مهم: مكان استضافة n8n
تبيّن أن n8n مستضاف على Hugging Face Spaces (`tasneemahmed-n8n.hf.space`) — وهي بيئة غير مخصصة لتحمّل حركة إنتاجية بمئات الطلبات المتزامنة (موارد محدودة، احتمال "cold start"، ووضع تشغيل n8n الافتراضي لا يوزّع الحمل على عدة workers). هذا على الأرجح السبب الأساسي للبطء الحالي، ولن يُحل بتحسينات الكود وحدها — يتطلب تغيير الاستضافة.

## المرفق: `LOAD_TESTING_PLAN.md`
خطة كاملة خطوة بخطوة تشمل: checklist اختبار وظيفي شامل لكل ميزة، طريقة تثبيت واستخدام أداة k6 لاختبار تحمل حقيقي (مع سكريبت جاهز يستهدف endpoints آمنة للقراءة)، كيفية قراءة وتفسير نتائج الاختبار، توصيات لتحسين استضافة n8n، وخطة إطلاق تدريجي آمنة بدل فتح البرنامج لـ300 مستخدم دفعة واحدة.

---

## تحديث: حقل اسم المستخدم + دعم تثبيت التطبيق (PWA)

### 1) حقل اسم المستخدم في صفحة التسجيل (signup.html)
- إضافة نص إرشادي واضح تحت الحقل يوضح القواعد المسموحة (إنجليزي فقط، أرقام، `_`، `-`، من 3 إلى 20 حرف)
- منع الكتابة بأي حرف غير مسموح به مباشرة أثناء الكتابة (تنظيف لحظي real-time)
- إضافة `pattern`/`minlength`/`maxlength`/`dir="ltr"`/`autocomplete="username"` على الحقل نفسه
- فحص تفصيلي جديد `Validators.usernameDetailed()` في `js/utils.js` بيرجع سبب الخطأ بالتحديد (فارغ / حروف ممنوعة / قصير / طويل) بدل رسالة عامة واحدة
- تصحيح `CONFIG.PATTERNS.USERNAME` في `js/config.js` ليفرض حد أقصى 20 حرف (كان غير محدود رغم إن رسالة الخطأ كانت بتقول 3-20)
- معالجة خاصة لخطأ "اسم المستخدم مكرر" القادم من السيرفر (Postgres unique constraint) بعرضه تحت الحقل نفسه بدل توست عام

### 2) دعم تثبيت التطبيق (Add to Home Screen / PWA)
- إضافة `manifest.json` في جذر المشروع + مجموعة أيقونات كاملة في `assets/icons/` (مبنية من شعار البرنامج المُرسل)
- إضافة `sw.js` (service worker بسيط بدون أي كاش، فقط لتحقيق شرط قابلية التثبيت)
- إضافة `js/pwa-install.js`: بانر مخصص بيظهر تلقائيًا يسأل المستخدم "هل تريد تثبيت التطبيق؟"، وبيستخدم شعار البرنامج، مع دعم خاص لتعليمات iOS اليدوية
- ربط favicon + apple-touch-icon + manifest في كل صفحات الموقع (13 صفحة)، فيظهر شعار البرنامج في تبويب المتصفح (الدومين) وعند التثبيت على الشاشة

---

## تحديث: إلغاء قيود رقم الهاتف المصري فقط
- كان فيه تحجيم في الكود بيقبل الأرقام المصرية بس (`CONFIG.PATTERNS.PHONE_EG` في `js/config.js`، مُستخدم في `Validators.phone` وفي فحص رقم الهاتف بصفحة `signup.html`)
- استبدلناه بنمط دولي عام (`CONFIG.PATTERNS.PHONE`) بيقبل أي رقم هاتف من أي دولة (مع أو من غير + ومفتاح الدولة)، والتحقق بقى بيتأكد إن عدد الأرقام الفعلي بين 7 و15 رقم بدل التقيد بصيغة مصر تحديدًا
- عدّلنا كمان `Formatters.formatPhone` في `js/utils.js` اللي كانت بتفرض صيغة `+20` المصرية على أي رقم (كانت دالة غير مستخدمة حاليًا في الواجهة، لكن تم تعميمها احتياطًا لأي استخدام مستقبلي)
- صفحة `login.html` وحقل هاتف الطالب في `students.html` مفيهمش أي تقييد من الأساس، فمحتاجوش تعديل

---

## ميزة جديدة: إشعارات Push يومية بحصص المعلم

- أُضيفت `js/notifications.js`: يربط جهاز المعلم بحسابه عبر OneSignal بعد تسجيل الدخول، ويعرض بانر اختياري لطلب إذن الإشعارات (بنفس أسلوب بانر تثبيت التطبيق)، ويربط زر الجرس 🔔 في الهيدر لطلب الإذن يدويًا
- تم ربط `js/sidebar.js` بالميزة الجديدة (تفعيل تلقائي عند رسم الهيدر، وفك الربط عند تسجيل الخروج من الأماكن الثلاثة التي تستدعي `Auth.signOut()`)
- أُضيف `CONFIG.PUSH_NOTIFICATIONS.ONESIGNAL_APP_ID` في `js/config.js` (يحتاج قيمة حقيقية من حساب OneSignal)
- أُضيف `n8n/send-daily-lesson-notifications.json`: workflow مجدول (Schedule Trigger) يعمل يوميًا الساعة 7 صباحًا بتوقيت القاهرة، يجلب حصص كل معلّم لهذا اليوم من `teachers_manager.schedules` (باحترام نوع التكرار: أسبوعي/يومي/شهري/مرة واحدة)، ويرسل إشعار Push عبر REST API الخاص بـ OneSignal لكل معلّم عنده حصة واحدة على الأقل
- **لا توجد أي migration جديدة لقاعدة البيانات** — OneSignal تدير الاشتراكات بنفسها عبر `external_id` المطابق لمعرّف المعلم في Supabase
- خطوات التفعيل الكاملة (إنشاء حساب OneSignal، الحصول على App ID و REST API Key، وربطهم بالفرونت إند وبـ n8n) موثقة بالتفصيل في `PUSH_NOTIFICATIONS_SETUP.md`

---

## إصلاح: إشعارات Push ما كانتش بتشتغل خالص (لا اشتراك ولا نافذة إذن)

**السبب:** `js/notifications.js` كان بيحمّل SDK بتاع OneSignal من رابط قديم
ومهجور (`cdn.onesignal.com/sdks/OneSignalSDK.js`)، وده رابط لنسخة "stub"
قديمة قبل v16 مالهاش أي علاقة بآلية `window.OneSignalDeferred` اللي باقي
الكود مبني عليها بالكامل. النتيجة كانت: كل استدعاءات `OneSignal.init()`،
`OneSignal.login()`، و`OneSignal.Notifications.requestPermission()` كانت
بتتحط في طابور انتظار وتفضل من غير تنفيذ أبدًا - من غير أي خطأ ظاهر في
الـ console، فمفيش نافذة إذن كانت بتظهر للمستخدم ومفيش أي اشتراك بيتسجّل.

**الإصلاح:** استبدلنا الرابط بالرابط الصحيح للنسخة الحالية v16:
`https://cdn.onesignal.com/sdks/web/v16/OneSignalSDK.page.js`

ملف `OneSignalSDKWorker.js` في جذر الموقع كان صحيحًا بالفعل ومحتاجش تعديل.

---

## إضافة: Google Tag Manager في كل صفحات الموقع

- أُضيف كود GTM الأول (`<script>...gtm.js...</script>`) فورًا بعد فتح تاج `<head>` في كل صفحة، بمعرّف `GTM-NXC2WZMT`
- أُضيف كود الـ noscript (`<iframe src="...ns.html?id=GTM-NXC2WZMT">`) فورًا بعد فتح تاج `<body>` في كل صفحة
- طُبّق على كل صفحات الموقع الـ13 (index, login, signup, reset-password, dashboard, students, student, schedule, books, settings, lesson, subscription, admin) — مفيش أي تعديل تاني على أي كود موجود
