# 📚 مكتبتي — النسخة 2 (دليل التشغيل)

## إيه الجديد؟

| الميزة | التفاصيل |
|---|---|
| **فتح الكتاب كامل** | الضغط على أي كتاب بيفتحه بصيغته الأصلية: PDF (PDF.js) · Word (docx-preview) · PowerPoint (pptx-preview) · صورة · نص. تنقّل بين الصفحات، تكبير/تصغير، تحميل الأصل، وزر "النص المستخرج". |
| **شات "اسأل الكتاب"** | زر صغير عائم جوه العارض بيفتح نافذة أسئلة عن **الكتاب ده بس**. السؤال بيتمسح بعد الإرسال، إيقاف/نسخ/إعادة محاولة، محادثة جديدة، سياق للمتابعة، وسؤال عن "الصفحة الحالية". أرقام المصادر `[مصدر N]` بتفتح مكانها في الكتاب. |
| **الكتب المصوّرة والممسوحة** | المتصفح بيستخرج النص ويفحص جودته صفحة صفحة. الصفحات الممسوحة/التالفة بتتقرأ بالذكاء الاصطناعي (Gemini) على دفعات صغيرة. الصفحات الفاضية بتتخطى. اللي اتقرأ بيتحفظ محليًا فلو المعالجة وقفت بتكمل من حيث توقفت. |
| **أنواع ملفات جديدة** | نص (`.txt`) وصور (JPG/PNG/WebP) — بجانب PDF/Word/PowerPoint. |
| **تقدّم حقيقي** | شريط تقدّم في بطاقة الكتاب + زر إلغاء + "إعادة المعالجة" للكتب المتوقفة/الفاشلة. |

## خطوات التفعيل (بالترتيب)

1. **Supabase → SQL Editor:** نفّذ `n8n/BOOKS_LIBRARY_V2_MIGRATION.sql` (بعد ما تكون نفّذت `BOOKS_LIBRARY_DB_MIGRATION.sql` القديم). آمن للتكرار.
2. **n8n → Import from file** للملفات دي (وفعّل كل واحد بعد الاستيراد):
   - جديدة: `book-ocr.json` · `book-index.json` · `finish-book.json` · `get-book-content.json`
   - معدّلة (استبدل القديمة): `process-book.json` · `ask-sources.json` · `create-book-source.json` · `list-books.json`
   - لو استوردت نسخة بنفس الـ path (`process-book`، `ask-sources`...) لازم تلغي/تمسح القديمة الأول عشان ما يحصلش تعارض في الـ webhook.
   - في كل Postgres node وكل HTTP node تأكد إن الـ credentials هي بتاعتك (الملفات بتشير لنفس أسماء الـ credentials الموجودة في باقي الـ workflows: `Postgres account` و`Gemini API Key`).
3. **انشر الواجهة** (Cloudflare Pages) بالمجلد كله. كل الصفحات اتعمل لها cache-busting (`?v=11`).
4. جرّب: ارفع PDF نصي صغير، بعدين كتاب ممسوح (أو صورة صفحة).

> الواجهة الجديدة **لا تعمل** قبل الخطوتين 1 و2 (رفع الكتب بيمرّ على الـ endpoints الجديدة).

## إزاي المعالجة بتشتغل

```
المتصفح                                     n8n / Supabase
───────                                     ──────────────
رفع الملف → Storage
process-book (start)  ─────────────────────► status=processing + مسح المقاطع القديمة
استخراج النص + فحص الجودة (PDF.js / ZIP)
صفحات ممسوحة → صور ─── book-ocr ───────────► Gemini vision → نص (مع حد يومي وإعادة الحصة عند الفشل)
تقطيع النص لمقاطع (بالفصل ورقم الصفحة)
كل 12 مقطع ──────────── book-index ────────► embedding لكل مقطع (gemini-embedding-001) → book_chunks
finish-book ───────────────────────────────► status=ready/failed
```

**مهم:** لازم الصفحة تفضل مفتوحة أثناء المعالجة (في تحذير لو حاولت تقفلها). لو اتقفلت الكتاب بيظهر "المعالجة توقفت" → اضغط **إعادة المعالجة**.

## الحدود وأماكن الضبط

| الإعداد | المكان | الافتراضي |
|---|---|---|
| أقصى حجم ملف | `CONFIG.BOOKS_SETTINGS.MAX_FILE_MB` (`js/config.js`) — وحد Supabase المجاني 50MB | 50 |
| أقصى صفحات تتقرأ بالـ OCR في كتاب واحد | `MAX_OCR_PAGES` | 400 |
| أقصى صفحات/شرائح للملف | `MAX_UNITS` | 1500 |
| صفحات الـ OCR في الطلب الواحد / مقاطع الفهرسة في الطلب | `OCR_BATCH_PAGES` / `INDEX_BATCH_CHUNKS` | 5 / 12 |
| حد الـ OCR اليومي لكل معلم | `dailyOcrPages` في node **Validate & Config** داخل `book-ocr.json` | 600 صفحة/يوم |
| حد أسئلة الشات اليومي | `dailyLimit` في `ask-sources.json` (مشترك مع باقي ميزات الـ AI) | 40 |
| أقصى طول للسؤال | `MAX_QUESTION_CHARS` + التحقق في `ask-sources.json` | 1000 حرف |
| موديل Gemini | `geminiModel` في `book-ocr.json` و`ask-sources.json` | `gemini-3.5-flash-lite` (زي باقي الـ workflows عندك) |

- **حجم الطلب في n8n:** دفعة الـ OCR (5 صور) ≈ 1–3 MB. حد n8n الافتراضي 16 MB (`N8N_PAYLOAD_SIZE_MAX`) كافٍ.
- **الـ embedding:** كل الـ workflows (`ask-sources` و`book-index` و`prepare-lesson`) بتستخدم `gemini-embedding-001` مع `outputDimensionality: 768` (نفس اللي اشتغل معاك؛ `text-embedding-004` مش شغال). ده متوافق مع `vector(768)` ومع الكتب المفهرسة قبل كده. لو غيّرت الموديل/الحجم لازم تغيّره في التلاتة وتعيد فهرسة الكتب. الـ workflow بيرفض حفظ أي embedding حجمه مش 768 (كود الخطأ `EMBED_DIM`).
- **حصة الـ embedding (مهم):** الفهرسة بتعمل طلب embedding **لكل مقطع** (واحد واحد بفاصل 1.1 ثانية، زي النسخة اللي اشتغلت، عشان ترتيب النتائج مضمون). المقاطع بحجم ≈1300 حرف. حسب المعلومات المتاحة عن الطبقة المجانية لـ `gemini-embedding-001` (مصادر ثانوية، راجع حصة مفتاحك في Google AI Studio): حوالي 100 طلب/دقيقة و30,000 token/دقيقة و**1,000 طلب/يوم**. يعني كتاب من 200 صفحة نصية ≈ 250–400 طلب، فالمجاني بيكفي لكام كتاب في اليوم بس. لو الحد وصل، المعالجة بتستنى وتعيد تلقائيًا (لحد 6 محاولات بفواصل أطول)، ولو وصلت لحد اليوم بتفشل برسالة وتقدر تعيدها بكرة (الـ OCR اللي اتقرأ محفوظ). الحساب المدفوع (Tier 1) حدوده أعلى بكتير.

## ملاحظات وقيود معروفة

- **Word/PowerPoint:** ملفات `.doc` و`.ppt` القديمة مش مدعومة (احفظها `.docx`/`.pptx`). ترقيم صفحات Word تقريبي (بناءً على فواصل الصفحات أو عدد الحروف)، فالاستشهاد في Word بيلوّن الجملة نفسها بدل الاعتماد على رقم الصفحة، وسؤال "الصفحة الحالية" غير متاح فيه.
- **الكتب الكبيرة جدًا** (أكثر من 1500 صفحة أو 400 صفحة ممسوحة) بتترفض برسالة توضح تقسيم الملف.
- **حقوق النشر:** أحيانًا Gemini بيرفض قراءة صفحات كتب منشورة (finish reason `RECITATION`). الصفحة دي بتتحسب "فاشلة"، والمعالجة بتكمل لو أقل من 40% من الصفحات فشلت وإلا بتفشل برسالة واضحة.
- **الأمان (موجود قبل التعديل):** كل الـ workflows بتفك الـ JWT من غير التحقق من توقيعه. راجع ده لأن endpoints الـ OCR بتستهلك حصة Gemini.
- مكتبات العرض بتتحمّل من jsDelivr وقت الحاجة فقط: `pdfjs-dist@5.6.205` · `docx-preview@0.3.7` · `pptx-preview@1.0.7` · `jszip@3.10.1`. لو فشل تحميل أي واحدة بيظهر خطأ واضح مع زر "عرض النص المستخرج".

## الملفات

- **جديدة:** `js/book-processor.js` · `js/book-viewer.js` · `js/book-chat.js` · `js/book-chunker.js` · `js/book-libs.js` · `js/md-lite.js` · `js/books-page.js` · `css/books.css` · `n8n/{book-ocr,book-index,finish-book,get-book-content}.json` · `n8n/BOOKS_LIBRARY_V2_MIGRATION.sql`
- **معدّلة:** `books.html` · `js/api.js` · `js/config.js` · `js/icons.js` · `n8n/{process-book,ask-sources,create-book-source,list-books}.json` — وكل الصفحات (رقم الإصدار `?v=11`).
